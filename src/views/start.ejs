<% if (onboarding) { %>
    <b>Welcome to Lightening Trade Bot <%= name %> </b>
<% } else { %>
    <b>Welcome back to Lightening Trade Bot <%= name %> </b>
<% } %>

Your mnemonic is <code><b><i><%= wallet.mnemonic %></i></b></code>, ensure to keep this safe and secure.

Your wallet address is `<code><b><i><%= wallet.address %></i></b></code>

Balance: <%= wallet.lastRecordedBalance %>